{
  "buildCommand": "cd frontend && CI=false npx craco build",
  "installCommand": "cd frontend && yarn install",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
