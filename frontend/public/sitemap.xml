<?xml version="1.0" encoding="UTF-8"?>
<!--
Sitemap index — points crawlers to the dynamic sitemap served by the backend.
The backend endpoint /api/sitemap.xml auto-includes:
  • Static pages (home, about, faq, privacy, terms, links)
  • All 85+ tool pages (/tool/:slug)
  • All published custom pages / articles (/p/:slug)
It uses the current host so it works on any deployed domain.
-->
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <sitemap>
    <loc>https://edm2n.com/api/sitemap.xml</loc>
  </sitemap>
</sitemapindex>
